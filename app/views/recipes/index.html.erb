<h1>Recipe Finder</h1>
<table>
  <% @recipes.each do |recipe| %>
    <tr>
      <td><%= link_to(image_tag(recipe["icon"], size: "100x100")) %></td>
      <td><%= link_to(recipe["title"], recipe["url"]) %></td>
      <td><%= recipe["social_rank"] %></td>
    </tr>
  <% end %>
</table>
